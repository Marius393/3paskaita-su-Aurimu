const celsius = prompt('Įveskite laipsnius');


const atsakymas = cToF(celsius);

alert(atsakymas)

function cToF(celsius) {
    return celsius * 1.8 + 32;
}

// celsius i fahrenheit



eur i rublius

function eurToRub(eur) {
    return eur * 88.05;
}